class NewsModel {
  String image_url, title, desc,content,source_name, source_url;
  DateTime date;

  NewsModel(this.title, this.desc,this.content, this.image_url, this.source_name,this.source_url, this.date);
}
